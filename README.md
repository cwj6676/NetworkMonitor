# NetworkMonitor

A network monitoring project that detects connectivity issues, records failures and recoveries, and tracks network status changes.

The project starts as a Python-based monitoring simulator and will later be expanded to monitor real network environments using ping, latency, packet loss, and service checks.
